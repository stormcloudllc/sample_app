<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section>
	<%= render 'shared/user_info' %>
      </section>
      <section>
	<%= render 'shared/micropost_form'%>
      </section>
    </aside>

    <div class="span8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center hero-unit">
	<h1>Hello, Beautiful Person!</h1>
	<h2>
		and <u>welcome</u>, to my 
		<b>realm</b>. ;D
	</h2>

	<%= link_to "Sign up now!", signup_path, :class => "bn btn-large btn-primary" %>
</div>

<%= link_to image_tag("rails.png", alt: "Rails"), 'http://rubyonrails.org/' %>
<% end %>
